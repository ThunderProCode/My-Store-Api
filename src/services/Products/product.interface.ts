export interface Product {
  id: string
  name: string,
  price: number,
  image: string,
  isBlock: boolean
}

export interface ProductData {
  name: string,
  price: number,
  image: string,
  isBlock:boolean
}
